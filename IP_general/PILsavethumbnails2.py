'''
> PIL save() method can save images in "most image file formats.
> thumbnail() method takes a tuple specifying the new size and converts the image
to a thumbnail image(A thumbnail is a small image that represents a larger image (when clicked on), and is often recognized with a border around it:)
with size that fits within the tuple.
'''
pil_im.thumbnail((128,128))
